events {
    worker_connections  1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;
    sendfile        on;
    keepalive_timeout  65;

    # Load our server configuration
    include c:/application/web/checkcc.live/nginx/default.conf;
}

